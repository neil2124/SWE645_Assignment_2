apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app  # This should match the label from the deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer  # This exposes the service externally
